@model IEnumerable<DAL.Models.Booking>
@{
    ViewData["Title"] = "My Bookings";
}

<h1>My Bookings</h1>
<p>
    <a asp-action="Create" class="btn btn-primary">New Booking</a>
</p>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Date</th>
            <th>Status</th>
            <th>Service</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Date.ToShortDateString()</td>
                <td>@item.Status</td>
                <td>@item.Service?.ServiceType</td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.BookingID" class="btn btn-sm btn-info">Details</a>
                    <form asp-action="Cancel" asp-route-id="@item.BookingID" method="post" style="display:inline">
                        @Html.AntiForgeryToken()
                        <button type="submit" class="btn btn-sm btn-danger"
                                onclick="return confirm('Cancel this booking?')">
                            Cancel
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>